---
description: 禁止提交密钥、录音和本机数据
alwaysApply: true
---

# 密钥与本机数据

- 永远不要 `git add .`。按路径添加文件。同学不要 `git push` 到 origin。
- 不要提交 `.env`、`.env.local`、`data/`、`keys-inbox/`、`*.wav`、`*.m4a`、`*.db`、`credentials.json`。不要把 Key 读完后复述到聊天里，不要拷到别的机器或写进会进 Git 的文件。
- `.env.example` 只允许占位符（`sk-your-key-here`）。每人自己申请 Key。
- 用户把密钥放进仓库 `keys-inbox/`（Git 忽略）并标明哪家、哪一项时：只改本地 `.env` 对应行；不要复述 Key，不要 commit `.env`。不要让用户把 Key 发到聊天里。没有 `keys-inbox` 材料时不要主动打开或覆盖已有 `.env`。不要用别人的 Key。
- 安装见 [docs/USAGE.md](docs/USAGE.md)；带用户点界面见 [docs/AGENT_GUIDE.md](docs/AGENT_GUIDE.md)；同学复制的提示词见 [docs/AGENT_PROMPT.md](docs/AGENT_PROMPT.md)。不要做成安装包分发。
- 误把密钥推进去了：立刻让用户轮换 Key，不要只靠 `git rm`。
