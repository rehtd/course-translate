---
description: 禁止提交密钥、录音和本机数据
alwaysApply: true
---

# 密钥与本机数据

- 永远不要 `git add .`。按路径添加文件。
- 不要提交、不要读取后复述、不要拷贝到别的机器：`.env`、`.env.local`、`data/`、`*.wav`、`*.m4a`、`*.db`、`credentials.json`。
- `.env.example` 只允许占位符（`sk-your-key-here`）。每人自己申请 Key，填进本地 `.env`。
- 安装见 [docs/USAGE.md](docs/USAGE.md)；带用户点界面见 [docs/AGENT_GUIDE.md](docs/AGENT_GUIDE.md)。不要做成安装包分发。
- 误把密钥推进去了：立刻让用户轮换 Key，不要只靠 `git rm`。
